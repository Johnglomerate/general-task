#!/bin/sh
HUSKY_SH="$(dirname "$0")/_/husky.sh"

if [ -f "$HUSKY_SH" ]; then
    . "$HUSKY_SH"
fi

cd "$(dirname "$0")/.."

if [ ! -x node_modules/.bin/lint-staged ]; then
    echo "Skipping lint-staged because frontend dependencies are not installed."
    exit 0
fi

yarn lint-staged
