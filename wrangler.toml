#:schema node_modules/wrangler/config-schema.json
name = "frontend"
compatibility_date = "2026-04-03"

[build]
command = "cd frontend && echo '@fortawesome:registry=https://npm.fontawesome.com/' > .npmrc && echo '//npm.fontawesome.com/:_authToken=${FONTAWESOME_NPM_AUTH_TOKEN}' >> .npmrc && npx -y yarn@1.22.22 install && npx -y yarn@1.22.22 build"

[assets]
directory = "./frontend/dist"
